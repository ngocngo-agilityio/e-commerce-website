// Library
import { defineStyleConfig } from '@chakra-ui/react';

export const FormLabel = defineStyleConfig({
  baseStyle: {
    color: 'formLabel',
    fontSize: 'md',
    lineHeight: 'none',
    fontWeight: 'medium',
    mb: '8px',
  },
});
