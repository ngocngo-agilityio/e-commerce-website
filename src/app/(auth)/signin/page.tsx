import { Center } from '@chakra-ui/react';

// Components
import { LoginForm } from '@components';

const SignIn = () => {
  return (
    <Center mt="200px">
      <LoginForm />
    </Center>
  );
};

export default SignIn;
